package first;

/* Napisz funkcję, która przyjmie 3 parametry:
 * godziny, minuty i sekundy i zwróci czas
 * w sekundach. */
public class Zadanie4 {
    public int getSeconds (int h, int m, int s){
        return (h*3600 + m*60 +s);
    }
}
